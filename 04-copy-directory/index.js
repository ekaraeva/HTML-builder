const fs = require('fs');
const path = require('path');
const mkdir = require('fs');
const copyFile = require('fs');


  fs.mkdir('D:\\htmlcss\\HTML-builder\\04-copy-directory\files-copy', (error) => {
    if(error) {
      throw error;
    }})
    

